

<div class="container mt-3">

  <h1>Edit ingredient in <%= @cocktail.name %></h1>

  <%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :amount %>
  <%= f.input :description %>
  <%= f.submit "update", class: "btn btn-primary" %>
  <% end %>

</div>
